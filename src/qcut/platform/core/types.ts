/**
 * Barrel re-export — types have been split into types/ directory.
 * Kept for backward compatibility so existing imports continue to work.
 *
 * @module @qcut/platform-core/types
 */

export * from "./types/index.js";
