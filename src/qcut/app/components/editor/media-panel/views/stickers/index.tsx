// Main export for stickers module
export { StickersView } from "./stickers-view";

// Re-export types if needed elsewhere
export type {
	StickerItemProps,
	CollectionContentProps,
	StickersViewProps,
	RecentSticker,
} from "./types/stickers.types";
